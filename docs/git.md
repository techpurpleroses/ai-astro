# Git Push — ai-astro repo

## Quick Push (copy & paste this every time)

```bash
cd "c:/Purple Roses Technology/SaaS Projects/Astro-AI/astro-ai-repo" && git add . && git commit -m "update" && git push


git add . ; git commit -m "Major Updates - Auth System AND Backend for DB." ;  git push --set-upstream origin main
```

---

## With a custom commit message

```bash
cd "c:/Purple Roses Technology/SaaS Projects/Astro-AI/astro-ai-repo" && git add . && git commit -m "your message here" && git push
```

---

## Check what changed before pushing

```bash
cd "c:/Purple Roses Technology/SaaS Projects/Astro-AI/astro-ai-repo" && git status
```

---

## Notes
- Remote is already authenticated — no login prompt will appear
- Account: `techpurpleroses` → repo: `ai-astro`
- Token: Classic PAT, **no expiration** — no need to ever regenerate
